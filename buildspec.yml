version: 0.2

phases:
  install:
    commands:
      - echo $CODEBUILD_SOURCE_VERSION
      - echo $CODEBUILD_WEBHOOK_BASE_REF
      - echo $CODEBUILD_WEBHOOK_TRIGGER
      - yarn
  build:
    commands:
      - yarn build
  post_build:
    commands:
      - echo build is `date`
artifacts:
  files:
    - "build/**/*"
  name: build-output
  discard-paths: no
  s3-prefix: code-build-outputs
  secondary-artifacts:
    cloudformationfile:
      files:
        - cloud-formtation.json
      name: cloudformtation
      discard-paths: no
